# midterm review

'''
montys python flying circus

else clause
    - for and while
    - not only do while, not only for
'''

a, b = 0, 1
a, b = b, a+b
print (a, b)
# (1, 1)

print([x**2 for x in range(3)])
# []

# tuples are immutable 

# list.pop() without an argument
# always pops back from end

# enumerate itrate both idx n value

# membership testing operator: 
# in

# 3 < 5 == 5
# T

# what is not allowed as a dict key
# list immutable mutable

# what built-in func pairs multiple sequences element-wise?
# zip

# what statement ab lambda func is correct
# defined using lambda keyword

# which func is used to rm dupes frm sequences?
# set()

# lexicographical ordering for string in python uses?
#  unicode code points

# a = (1,2, [3,4]) a[2].append(5)
# print(a)

# is this t/f [1,2,3] < [1,2,4]
#T

#x = set("banana") print(sorted(x))
# ['a', 'b', 'b']

# valid?
# (1,2,3)[0]

#

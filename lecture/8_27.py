'''
Python 
    - high lvl lang with high lvl data types built-in:
        - flexible arrs
        - dicts
        - lists
    - allows for modularity
        - lrg collection of std modules
        - programmer defined modules can b reused in other programs
            - reusability
    - interpreted lang
        - no compilation/linking req
        - less dev time
        - exec faster than UNIX, slower than compiled binary
        - interactive interpreter for quick code
    - shorter code
        - complex ops in single statement
        - no var declaration needed
        - no ;
    - extensible
        - open source
    - named after BBC show 'Monty Python's Flying Circus'
    - dynamically typed - var type changes based on data type held
'''

# importing modules
import math
import math as M
from math import factorial
from math impirt *

'''
style conventions:
    - ClassNames
    - function_names
    - other_names
    - _name = weak form of privatization
        - for classes, intended for internal use only
    - _private_func = won't be imported
    - __name__ = special function
    - name_ = to avoid naming conflicts ex. keywords
    
keywords:
    - python lang reference sec. 2.3.1

comments: use #

interpreter:
    $ python3.9 to start
    
    >>> CTRL+D to exit
    >>> quit()

    >>> ^ interactive editing use arrow keys
    >>> v 
    >>> <- arrows
    ... continuation lines
    
    acts like a simple calc
'''

'''
numbers:
    / float div
    // floor (int) div
    % mod
    ** powers

strings:
    '' or ""
    \ to esc quotes
    raw string uses r before quote
    can be concatenated with + operator
     "  "  repeated with * operatr
    can be indexed, sliced
    len() f(x) returns length of str
'''

'''
